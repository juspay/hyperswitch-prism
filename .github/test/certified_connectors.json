{
  "_note": "Scenarios run against live connector sandboxes in CI. See .github/scripts/certify-connectors.sh for how a failure is attributed. An entry requires working credentials in the CI creds file.",
  "connectors": [
    {
      "name": "stripe",
      "suite": "PaymentService/Authorize",
      "scenario": "no3ds_auto_capture_credit_card",
      "skip_dependencies": true
    }
  ]
}
