name: Create a nightly tag

on:
  schedule:
    - cron: "0 0 * * 1-5" # Run workflow at 00:00 midnight UTC (05:30 AM IST) every Monday-Friday

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  create-nightly-tag:
    name: Create a nightly tag
    permissions:
      contents: write
    uses: juspay/hyperswitch/.github/workflows/release-nightly-version-reusable.yml@843d9ce8669f8f97ccb66d8d970bc726f5de44b1
    secrets:
      token: ${{ secrets.CONNECTOR_SERVICE_CI_PAT }}
